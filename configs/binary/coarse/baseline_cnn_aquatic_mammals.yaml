architecture: baseline_cnn
run_name: baseline_cnn_lr_0.0001
seed: 42

task:
  type: binary
  label_level: coarse
  positive_label_names:
    - aquatic_mammals

validation:
  fraction: 0.1
  stratify: true

class_imbalance:
  strategy: class_weights

batch_size: 64
shuffle_buffer: 4096
dropout: 0.3
epochs: 20
optimizer: adam
learning_rate: 0.0001

early_stopping:
  monitor: val_loss
  patience: 5

output_dir: results/aquatic_mammals
save_weights: true
subset_size: null
